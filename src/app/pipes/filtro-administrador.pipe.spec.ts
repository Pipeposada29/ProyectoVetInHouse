import { FiltroAdministradorPipe } from './filtro-administrador.pipe';

describe('FiltroAdministradorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAdministradorPipe();
    expect(pipe).toBeTruthy();
  });
});
